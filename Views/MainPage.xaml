<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:CozyPlayer.Views"
             x:Class="CozyPlayer.Views.MainPage">

    <VerticalStackLayout Padding="10">

        <Label Text="CozyPlayer 🎵" FontSize="24" HorizontalOptions="Center" />

        <Button Text="🔄 Refresh" 
            Clicked="OnRefreshClicked" 
            HorizontalOptions="Center" />

        <Button Text="➕ Add file"
        Clicked="OnAddFileClicked"
        HorizontalOptions="Center" />
        
        <CollectionView ItemsSource="{Binding Tracks}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="10" Margin="5">
                        <StackLayout Orientation="Horizontal">
                            <Label Text="{Binding Title}" FontAttributes="Bold" WidthRequest="150"/>
                            <Button Text="▶" Command="{Binding Source={RelativeSource AncestorType={x:Type local:MainPage}}, Path=BindingContext.PlayCommand}" CommandParameter="{Binding}"/>
                            <Button Text="🗑" Command="{Binding Source={RelativeSource AncestorType={x:Type local:MainPage}}, Path=BindingContext.DeleteCommand}" CommandParameter="{Binding}"/>
                        </StackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>
</ContentPage>